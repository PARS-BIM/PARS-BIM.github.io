---
title: Easy connect (Multiple)
---

# Easy connect (Multiple)
With this command, you can connect multiple MEP elements much more easily, without the need to switch to 2D views or create sections. Instead, you can establish the connections directly in the 3D view, making the process faster and more convenient. 


## Steps

First, select the first group of elements, which can be considered the main runs, and then select the second group of elements, which can be considered the branches.

In the opened window, specify the angle of the elbow or tee fitting that will be placed on the main run, as well as the angle of the elbow that will be placed on the branch. Then click “OK.”

Note: If you want the connecting pipe between the branch and the main run to be removed after the connection is created, enable the “Delete Connecting Pipe” option.

Note: If you want the connection angle and configuration of each branch to be adjusted individually, disable the “Apply for all?” option.

Note: Branches are connected to the main runs based on their System Type. Only elements with matching System Types will be connected to each other.

Note:

If a branch connects before the end of the main run, the connection will be created using one elbow on the branch and one tee fitting on the main run.

If a branch connects beyond the end of the main run, the connection between the two elements will be created using two elbows.

<img src="https://pars-bim.github.io/docs/Assets/Easy-connect-Multiple.jpg" alt="Easy connect multiple elements" width="600">



Here is the short video to show the process: 

<iframe width="560" height="315" src="https://www.youtube.com/embed/gmoon5RPqg0?si=NAqfzLi02EDAGUNW" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
